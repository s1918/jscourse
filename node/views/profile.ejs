<!DOCTYPE html>
<html>
    <head>
        <style>
            body{background: skyblue; font-family: Verdana, Geneva, Tahoma, sans-serif; color: #fff; padding: 30px;}
            h1{font-size: 48px; text-transform: uppercase; letter-spacing: 2px; text-align: center;}
            h2{font-size: 30px; text-transform: uppercase; letter-spacing: 2px; text-align: center;}
            p, li{font-size: 25px; text-align: center; text-transform: capitalize; list-style-position: inside;}
            ul{padding-right: 2%;}
        </style>
    </head>
    <body>
        <%- include('partials/nav.ejs'); %>
        <h1>welcome to the page of <%= person %></h1>
        <p><strong>age: </strong><%= data.age %></p>
        <p><strong>job: </strong><%= data.job %></p>
        <h2>Hobbies</h2>
        <ul>
            <% data.hobbies.forEach(function(item){ %>
                <li><%= item %></li>
            <% }); %>
        </ul>
    </body>
</html>